# Base URL for the Nature Runner API.
# In local dev point this at your Express server.
# In production on Render this can be left unset — the client is served
# from the same origin as the API so relative URLs work.
VITE_API_ROOT=http://localhost:3000/api/v1/
